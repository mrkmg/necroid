diff --git a/zombie/core/skinnedmodel/ModelManager.java b/zombie/core/skinnedmodel/ModelManager.java
index e921341..e15b4bb 100644
--- a/zombie/core/skinnedmodel/ModelManager.java
+++ b/zombie/core/skinnedmodel/ModelManager.java
@@ -390,9 +390,10 @@ public final class ModelManager {
    }
 
    public void RenderParticles(TextureDraw var1, int var2, int var3) {
-      int var4 = ParticlesFire.getInstance().getFireShaderID();
-      int var5 = ParticlesFire.getInstance().getSmokeShaderID();
-      int var6 = ParticlesFire.getInstance().getVapeShaderID();
+      // Necroid per-tile path: var3, var1.d encode (tileX+1, tileY+1).
+      int tileX = var3 - 1;
+      int tileY = var1.d - 1;
+      if (tileX < 0 || tileY < 0) return;
 
       try {
          Util.checkGLError();
@@ -404,29 +405,25 @@ public final class ModelManager {
       GL11.glMatrixMode(5889);
       GL11.glPushMatrix();
       GL11.glLoadIdentity();
-      GL11.glViewport(
-         0, 0, SpriteRenderer.instance.getRenderingPlayerCamera(var2).OffscreenWidth, SpriteRenderer.instance.getRenderingPlayerCamera(var2).OffscreenHeight
-      );
       GL11.glMatrixMode(5888);
       GL11.glPushMatrix();
       GL11.glLoadIdentity();
-      float var7 = ParticlesFire.getInstance().getShaderTime();
-      GL11.glBlendFunc(770, 1);
-      ARBShaderObjects.glUseProgramObjectARB(var4);
-      Shader var8 = Shader.ShaderMap.get(var4);
-      if (var8 instanceof FireShader) {
-         ((FireShader)var8).updateFireParams(var1, var2, var7);
-      }
-
-      ParticlesFire.getInstance().getGeometryFire(var3);
-      GL11.glBlendFunc(770, 771);
-      ARBShaderObjects.glUseProgramObjectARB(var5);
-      var8 = Shader.ShaderMap.get(var5);
-      if (var8 instanceof SmokeShader) {
-         ((SmokeShader)var8).updateSmokeParams(var1, var2, var7);
+      GL11.glDisable(2929); // GL_DEPTH_TEST off
+
+      java.util.List<zombie.iso.Particles> emitters = zombie.iso.NecroidEmitterRegistry.emitters();
+      for (int i = 0, n = emitters.size(); i < n; i++) {
+         zombie.iso.Particles p = emitters.get(i);
+         int sid = p.getShaderID();
+         if (sid == 0) continue;
+         GL11.glBlendFunc(p.getBlendSrc(), p.getBlendDst());
+         ARBShaderObjects.glUseProgramObjectARB(sid);
+         zombie.iso.NecroidParticleShader sh = p.getShader();
+         ARBShaderObjects.glUniformMatrix4fvARB(sh.mvpMatrix, true, p.getMVPMatrix());
+         ARBShaderObjects.glUniform1fARB(sh.currentTime, p.getShaderTime());
+         ARBShaderObjects.glUniform1fARB(sh.lifetime, p.getLifetime());
+         p.getGeometryForTile(tileX, tileY);
       }
 
-      ParticlesFire.getInstance().getGeometry(var3);
       ARBShaderObjects.glUseProgramObjectARB(0);
       GL11.glMatrixMode(5888);
       GL11.glPopMatrix();
@@ -435,9 +432,6 @@ public final class ModelManager {
       GL11.glPopAttrib();
       GL11.glPopClientAttrib();
       Texture.lastTextureID = -1;
-      GL11.glViewport(
-         0, 0, SpriteRenderer.instance.getRenderingPlayerCamera(var2).OffscreenWidth, SpriteRenderer.instance.getRenderingPlayerCamera(var2).OffscreenHeight
-      );
       if (!PZGLUtil.checkGLError(true)) {
          DebugLog.General.println("DEBUG: EXCEPTION RenderParticles");
          PZGLUtil.printGLState(DebugLog.General);
@@ -1614,7 +1608,7 @@ public final class ModelManager {
          var2.setPriority(var2 == this.m_gameAnimations ? 0 : -1);
       }
 
-      ArrayList var12 = ScriptManager.instance.getAllAnimationsMeshes();
+      ArrayList<AnimationsMesh> var12 = ScriptManager.instance.getAllAnimationsMeshes();
       ArrayList var13 = zombie.ZomboidFileSystem.instance.getModIDs();
 
       for (int var3 = 0; var3 < var13.size(); var3++) {
@@ -1683,7 +1677,7 @@ public final class ModelManager {
    }
 
    public void initAnimationMeshes(boolean var1) {
-      ArrayList var2 = ScriptManager.instance.getAllAnimationsMeshes();
+      ArrayList<AnimationsMesh> var2 = ScriptManager.instance.getAllAnimationsMeshes();
 
       for (AnimationsMesh var4 : var2) {
          ModelMesh.MeshAssetParams var5 = new ModelMesh.MeshAssetParams();
